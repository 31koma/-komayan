{
  "name": "komayan-portfolio-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/prepare-sites-build.mjs",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@openai/sites-vite-plugin": "0.2.0",
    "astro": "^7.0.7",
    "fast-xml-parser": "^5.11.1",
    "typescript": "^6.0.3"
  }
}
